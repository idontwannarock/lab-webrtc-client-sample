# WebRTC 視訊聊天室 - Docker 環境配置

# Janus Gateway 設定
JANUS_WS_PORT=8188          # WebSocket API 端口
JANUS_HTTP_PORT=8081        # HTTP API 端口 (JavaScript 庫)
JANUS_ADMIN_WS_PORT=7088    # 管理 WebSocket 端口
JANUS_ADMIN_HTTP_PORT=7188  # 管理 HTTP 端口

# 客戶端網頁服務
WEB_PORT=8080               # 客戶端應用端口

# 管理界面 (可選)
ADMIN_UI_PORT=7080          # 管理界面端口

# 網路設定
COMPOSE_PROJECT_NAME=webrtc-chat

# 使用方法:
# 1. 複製此文件為 .env:
#    cp .env.example .env
# 
# 2. 根據需要修改端口設定
# 
# 3. 如果修改了端口，記得同步更新 index.html 中的配置:
#    - 第 401 行: server: 'ws://localhost:8188'
#    - 第 242 行: src: 'http://localhost:8081/janus.js'